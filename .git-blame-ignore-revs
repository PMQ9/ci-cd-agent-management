# Revisions that are pure mechanical reformats (no logic changes). Skipped by
# `git blame` so the original authoring commit shows through.
#
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# (GitHub applies it automatically.)

# style: reformat codebase with Biome (bulk, mechanical)
e2108f44cfe1b5681008ecf0aa90f8c0c8b0c654
